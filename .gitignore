# Environment & secrets
.env
.env.local
*.key
secrets/

# Python — build, cache, package metadata
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# Distribution / build
build/
dist/
*.egg

# Virtual environments
venv/
.venv/
env/
ENV/

# Jupyter
.ipynb_checkpoints/

# OS
.DS_Store
Thumbs.db

# IDE
.idea/
.vscode/
*.swp
*.swo
*~

# Tooling / local-only docs (intentionally not versioned)
docs/superpowers/
Docs/Brainstorming/

# Bootstrap PoC runs (regenerated by run_poc.py; older dev iterations excluded,
# only the latest reference runs are kept for comparison with the README).
mc_regime/outputs/runs/poc/
mc_regime/outputs/runs/poc_beer/
mc_regime/outputs/runs/poc_eta/
mc_regime/outputs/runs/poc_final/  # reference GO run (cross-pool, B=1999)
mc_regime/outputs/runs/poc_fvg/
mc_regime/outputs/runs/poc_gated/
mc_regime/outputs/runs/poc_l4fix/
mc_regime/outputs/runs/poc_overnight/  # reference v8 GO run
mc_regime/outputs/runs/poc_test/
mc_regime/outputs/runs/poc_v4/
mc_regime/outputs/runs/poc_v5/
mc_regime/outputs/runs/poc_v6/
mc_regime/outputs/runs/poc_v7/
mc_regime/outputs/runs/poc_v8/  # reference v8 with CODE_REVIEW.md
mc_regime/outputs/runs/poc_v10/  # reference v10 with cleaned-up code

# Notion exports (re-imported on demand, not part of the code)
# Matches "Ökonomische Regime — *.md" exported from Notion.
Ökonomische Regime *.md

# Raw FX data (large; download with mc_regime/scripts/load_fx_data.py)
dukascopy_data/

# Economic calendar CSVs (download with mc_regime/scripts/load_economic_calendar.py)
Economic-Calendar/

# Local root-level Notion exports (work in docs/brainstorming/ instead)
"Regime-Preserving Bootstrap" *.md
eurusd-m30-bid-*.csv
